"""Utility helpers for the search app."""
from typing import Iterable, Any, List


def simple_rank(results: Iterable[Any]) -> List[Any]:
    """Return results as a list; replace with ranking logic."""
    return list(results)
